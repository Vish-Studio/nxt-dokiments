import { createTemplate } from "@/lib/market-place/create-template";
import { contract } from "@/lib/market-place/documents";
import { modern } from "@/lib/market-place/styles";

export const modernContract = createTemplate(modern, contract);
