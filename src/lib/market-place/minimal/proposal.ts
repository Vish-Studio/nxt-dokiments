import { createTemplate } from "@/lib/market-place/create-template";
import { proposal } from "@/lib/market-place/documents";
import { minimal } from "@/lib/market-place/styles";

export const minimalProposal = createTemplate(minimal, proposal);
